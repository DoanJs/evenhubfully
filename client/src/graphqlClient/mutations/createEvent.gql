mutation CreateEvent($eventinput: EventInput!) {
  createEvent(eventinput: $eventinput) {
    EventID
    title
    description
    locationTitle
    locationAddress
    imageUrl
    price
    category
  }
}
